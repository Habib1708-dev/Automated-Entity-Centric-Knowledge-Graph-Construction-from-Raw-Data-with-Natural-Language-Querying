# Model presets: named sets of settings for the three kinds of run.
#
# Choose one per command with `uv run kg --preset <name> ...`, or as a default with KG_PRESET=<name> in .env.
# Priority: a variable set in the terminal > the preset > .env > the defaults in config.py, so a preset
# replaces the model lines of .env, and one value can still be overridden for a single run.
# Keys are Settings fields (see src/kgbuilder/config.py); an unknown key or preset is an error, not ignored.
# Never put an API key here: this file is committed.
# smoke and dev read a small committed subset of data/ (samples/), quality the whole dataset.

# Free check that the code runs end to end, on a tiny subset (one product, three reviews: about 10 LLM
# requests). Uses GEMINI_FREE_API_KEY, a key from a Google project without billing: $0, but capped per day,
# which also keeps smoke runs rare. The graph it produces says nothing about quality.
smoke:
  description: gemini-3.5-flash-lite on the free key, samples/smoke, $0
  llm_provider: gemini
  gemini_key: free
  schema_model: gemini-3.5-flash-lite
  extract_model: gemini-3.5-flash-lite
  embed_model: gemini-embedding-001
  data_dir: samples/smoke
  # how data_dir is made from the full dataset: `uv run kg sample` rewrites it (see sampling.py)
  sample:
    source: data
    root: {file: products.csv, column: product_id, values: [P-1000]}
    documents:
      - product_reviews/stockholm_chair_reviews.md
    sections: 3
  mlflow_experiment: kgbuilder-smoke

# Cheap check with real Gemini behaviour on the paid key (API errors, schemas, tracking), on a subset of
# three products. Flash-Lite does not think, which is where the cost of Flash goes. On the whole of data/
# it cost $0.06 (2026-09-22); the subset needs about a third of that. Not for reported results.
dev:
  description: gemini-3.5-flash-lite everywhere, samples/dev, about 2 cents per run
  llm_provider: gemini
  gemini_key: paid
  schema_model: gemini-3.5-flash-lite
  extract_model: gemini-3.5-flash-lite
  embed_model: gemini-embedding-001
  data_dir: samples/dev
  # how data_dir is made from the full dataset: `uv run kg sample` rewrites it (see sampling.py)
  sample:
    source: data
    root: {file: products.csv, column: product_id, values: [P-1000, P-1003, P-1007]}
    documents:
      - product_reviews/stockholm_chair_reviews.md
      - product_reviews/uppsala_sofa_reviews.md
      - product_reviews/helsingborg_dresser_reviews.md
    sections: 4
  mlflow_experiment: kgbuilder-dev

# Genuine quality and performance runs: the numbers reported in the thesis come from this preset only.
# Gemini 3.8 Flash for every role: it scores higher than 3.1 Pro (Artificial Analysis index 41 vs 30) at
# less than half the price. Without a thinking level it cost $1.25 per run on data/ (2026-09-22), $1.00 of
# it thinking during extraction; the levels below cap that. Extraction and adjudication are many small
# tasks (low); the plan and the text schema are one hard design task each (medium).
quality:
  description: gemini-3.8-flash everywhere, thinking medium for schema work and low for extraction, data/
  # a run with this preset is a comprehensive test: Claude Code asks the user before starting one
  # (.claude/hooks/run_guard.py). A person typing the command is not asked.
  ask_permission: true
  llm_provider: gemini
  gemini_key: paid
  schema_model: gemini-3.8-flash
  extract_model: gemini-3.8-flash
  schema_thinking: medium
  # low kept after the R31 comparison (2026-09-22): medium reached the same validated precision and
  # recall (1.00 / 1.00 on the 96-fact gold set) and exact-match F1 0.273 against 0.265, for 4.2x the
  # extraction cost ($0.354 against $0.085, 1 064 thinking tokens per call against 37). Override per run
  # with EXTRACT_THINKING=medium; see docs/evaluation/results_2026-09-22_r31.md.
  extract_thinking: low
  embed_model: gemini-embedding-001
  data_dir: data
  mlflow_experiment: kgbuilder
