"""Text path: documents -> chunks -> lexical graph -> text schema -> evidence-backed facts.

Order of use: documents -> chunking -> lexical (write, then read back) -> schema (LLM) -> extraction (LLM)
-> subject_graph.
"""
